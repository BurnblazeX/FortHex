{
  "name": "forthex",
  "version": "0.0.0",
  "private": true,
  "description": "FortHex - build tooling only. The game itself is plain scripts in js/; this exists solely to compile the React UI in src/ui/ into dist/ui-bundle.js.",
  "scripts": {
    "build": "node tools/build-ui.js",
    "watch": "node tools/build-ui.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
